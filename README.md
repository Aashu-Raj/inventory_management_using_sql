# Inventory Analytics
This project implements a complete **inventory data processing and analytics workflow** for retail inventory management. It ingests raw CSV data, transforms it through a structured **Medallion Architecture (Bronze/Silver)**, normalizes it into a relational schema, and produces actionable business insights through SQL-based analytical queries and a Power BI dashboard.
